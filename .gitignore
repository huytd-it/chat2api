__pycache__/
*.egg-info/
.env
.venv/
.pytest_cache/
**/auth/
**/secrets/
# Kho account dùng chung: chứa cookie phiên đăng nhập, không được commit.
**/.accounts/
# DB SQLite, Chromium profile và blob đính kèm (xem docs/design-v2.md §1).
# `data-*/` phủ mọi biến thể data dir do CHAT2API_DATA_DIR sinh ra khi chạy thử
# (data-profile/, data-test/...) — chúng chứa cookie đăng nhập, không được commit.
data/
data-*/
.superpowers/
*.log